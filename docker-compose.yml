services:
  server:
    build:
      context: ./Server
    ports:
      - "5000:5000"
    env_file:
      - ./Server/.env
    restart: always

  client:
    build:
      context: ./Client
    ports:
      - "80:80"
    restart: always
    depends_on:
      - server
